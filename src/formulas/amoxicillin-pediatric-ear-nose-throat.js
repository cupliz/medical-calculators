import React from 'react'

export const formula = (weight, dosage) => {
  // Dose = Weight * Dosage
  return weight * dosage
}

const Formula = props => (
  <div>
    inside Formula
  </div>
)

export default Formula

